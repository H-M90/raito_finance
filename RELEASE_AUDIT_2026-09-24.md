# فحص جاهزية النشر — 24 سبتمبر 2026

## القرار

**لا يُعتمد النشر بعد.** الكود والاختبارات المحلية يمران، لكن بيئة النشر الفعلية لم تُفحص، والبيئة المتاحة الآن تعمل بـ `APP_ENV=local` و`APP_DEBUG=true`. كما لم تُثبت نسخة احتياطية قابلة للاستعادة أو تشغيل العامل والجدولة في بيئة النشر.

## ما تم التحقق منه

| البوابة | النتيجة | الدليل والحدود |
| --- | --- | --- |
| اختبارات PHP | نجح | 92 اختبارًا، 353 تأكيدًا، بلا إخفاق أو risky. الاختبارات تستخدم SQLite. |
| توافق PHP | نجح | فحص PHP 8.2 و312 ملف PHP بلا أخطاء صياغة. |
| المتطلبات الثابتة | نجح | 100/100 بعد تحديث المطابقات النصية وإتاحة تقرير الربحية طوال التعامل. الفحص الثابت لا يحل محل اختبار المستخدم. |
| الواجهة والأصول | نجح جزئيًا | `npm run check` و`npm run build`؛ أصول JS/CSS المنشورة مطابقة للمصدر. عرض شاشتي الصلاحيات والعملاء بصريًا داخل متصفح مصادق عليه لم يُختبر. |
| اعتماديات PHP | نجح جزئيًا | أُنشئ `composer.lock` وثُبتت نسخته، وفحص متطلبات المنصة نجح. `composer audit --locked` لم يجد نشرات ثغرات. `composer validate` ناجح مع تحذير للنسخة المثبتة عمدًا من ZipStream؛ الوضع strict يفشل لهذا التحذير. |
| اعتماديات JS | نجح | `npm audit --omit=dev` أبلغ عن صفر ثغرات. |
| قاعدة البيانات الحالية | نجح | طُبقت هجرة إصدار الجلسات الجديدة على قاعدة المشروع المحلية؛ كانت الهجرات السابقة الـ34 مطبقة. سبعة فحوص قراءة فقط للروابط والأرصدة والعملات كلها صفر مخالفات. هذا ليس اختبار إنشاء قاعدة جديدة على خادم النشر. |
| التشغيل المحلي | نجح جزئيًا | `GET /up` و`GET /login` أعادا 200؛ المسارات المحمية أعادت 302 إلى الدخول. لم تُنفذ رحلة مستخدم مصادق عليه في متصفح حقيقي. |
| Queue والجدولة | نجح جزئيًا | `queue:failed` بلا وظائف فاشلة و`schedule:list` يعرض المهام. استمرار العامل والجدولة في بيئة النشر لم يُثبت. |
| جاهزية الإنتاج | **فشل** | `verify-production-readiness.php` يرفض `APP_DEBUG=true` ويحذر من `APP_ENV=local` في البيئة المتاحة. إعدادات خادم الإنتاج غير متاحة للفحص. |
| المصدر وCI | غير مثبت | المجلد الحالي بلا `.git`، لذلك لم يمكن فحص التغييرات كـ commit أو تشغيل CI على نفس النسخة. عُدلت CI لتثبيت `composer.lock` بدل توليده أثناء التشغيل. |

## إصلاحات شملها الفحص

- تصحيح تحميل منتجات العقود، حفظ عروض الأسعار، تواريخ الاستحقاق، حساب الربحية الصافية، وعدم إعادة بناء الاستحقاقات عند تعديل بيانات تشغيلية.
- إتاحة تقرير ربحية العميل/العقد طوال التعامل مع فصل العملة وظهور العقود الملغاة.
- تصحيح مربع اختيار الصلاحيات في RTL باستخدام checkbox أصلي وحالة تركيز مرئية، وإزالة `onclick` من القالب.
- توحيد أيقونات إجراءات العملاء، وزيادة الفلاتر على بيانات المدينة ومسؤول المبيعات والعقود وحالة المتابعة والتصنيفات الفعلية، واستبدال نافذة إعادة التقييم بنافذة قابلة للإغلاق من الزر والخلفية وEsc.
- إبطال جلسات المستخدم القديمة ورمز «تذكرني» عند تعطيله، ومنع عودة الجلسات القديمة بعد إعادة التنشيط.
- إصلاح تهيئة اختبارات PHPUnit، ومدقق توافق PHP 8.2، ومطابقات مدقق المتطلبات.
- استعادة ملفات الهجرات الناقصة من `D:\projects\raito-finance.zip` دون استبدال الهجرة الجديدة.

## بوابات واجبة قبل قرار النشر

1. فحص نسخة النشر نفسها على خادم تجهيز أو إنتاج بإعدادات `APP_ENV=production` و`APP_DEBUG=false` و`APP_URL` الصحيح عبر HTTPS، ثم تشغيل فحص جاهزية الإنتاج هناك.
2. تشغيل CI على commit محدد يحتوي `composer.lock` والأصول المبنية والهجرات، وإعادة اختبارات PHP والبناء بعد `composer install` و`npm ci` من الصفر.
3. أخذ نسخة احتياطية من قاعدة البيانات والملفات الخاصة، وتجربة الاستعادة، ثم التحقق من الهجرات على نسخة من بيانات الإنتاج.
4. إثبات تشغيل Queue worker وscheduler والبريد والتخزين الخاص، ومراجعة السجلات بعد التشغيل.
5. اختبار مسارات المستخدم في متصفح مصادق عليه: العروض → العقود → الاستحقاقات → التحصيل → التقارير، والاستيراد وكشف البنك والصلاحيات. راجع شاشة الصلاحيات على الهاتف وسطح المكتب.

لا يمثل هذا التقرير موافقة على النشر أو اختبارًا لبيئة الإنتاج.
